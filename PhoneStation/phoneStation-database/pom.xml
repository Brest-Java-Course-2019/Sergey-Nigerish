<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.epam.brest.project.ps</groupId>
        <artifactId>phone-station</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>PhoneStation-database</artifactId>

    <dependencies>
        <dependency>
            <groupId>com.epam.brest.project.ps</groupId>
            <artifactId>model</artifactId>
        </dependency>
        <dependency>
            <groupId>com.epam.brest.project.ps</groupId>
            <artifactId>test-db</artifactId>
        </dependency>
        <dependency>
            <groupId>com.epam.brest.project.ps</groupId>
            <artifactId>dao</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <version>${org.postgresql.version}</version>
        </dependency>
    </dependencies>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <org.postgresql.version>42.2.5</org.postgresql.version>
        <org.flywaydb.version>5.2.4</org.flywaydb.version>
    </properties>

    <profiles>
        <profile>
            <id>local</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <serverName>localhost</serverName>
                <portNumber>5432</portNumber>
                <dbName>phonestation</dbName>
                <userName>psdba</userName>
                <password>admin123</password>
            </properties>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-maven-plugin</artifactId>
                <version>${org.flywaydb.version}</version>
                <configuration>
                    <driver>org.postgresql.Driver</driver>
                    <url>jdbc:postgresql://${serverName}:${portNumber}/${dbName}</url>
                    <user>${userName}</user>
                    <password>${password}</password>
                    <table>schema_version</table>
                    <sqlMigrationSeparator>__</sqlMigrationSeparator>
                    <encoding>UTF-8</encoding>
                    <placeholderReplacement>false</placeholderReplacement>
                    <validateOnMigrate>true</validateOnMigrate>
                    <cleanOnValidationError>false</cleanOnValidationError>
                    <ignoreFutureMigrations>false</ignoreFutureMigrations>

                    <cleanDisabled>false</cleanDisabled>
                    <skip>false</skip>
                </configuration>
                <dependencies>
                    <dependency>
                        <groupId>org.postgresql</groupId>
                        <artifactId>postgresql</artifactId>
                        <version>${org.postgresql.version}</version>
                    </dependency>
                </dependencies>
            </plugin>
        </plugins>
    </build>

</project>